document.addEventListener("DOMContentLoaded", () => {
  document.querySelectorAll(".ps-alpha").forEach((a) => {
    a.addEventListener("input", () => {
      a.value = a.value.replace(/[^a-z]/gi, "");
    });
  }),
    document.querySelectorAll(".ps-alpha-u").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z]/gi, "").toUpperCase();
      });
    }),
    document.querySelectorAll(".ps-alpha-l").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z]/gi, "").toLowerCase();
      });
    }),
    document.querySelectorAll(".ps-alphaspace").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z ]/gi, "");
      });
    }),
    document.querySelectorAll(".ps-alphaspace-u").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z ]/gi, "").toUpperCase();
      });
    }),
    document.querySelectorAll(".ps-alphaspace-l").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z ]/gi, "").toLowerCase();
      });
    }),
    document.querySelectorAll(".ps-alphanumeric").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9]/gi, "");
      });
    }),
    document.querySelectorAll(".ps-alphanumeric-u").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9]/gi, "").toUpperCase();
      });
    }),
    document.querySelectorAll(".ps-alphanumeric-l").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9]/gi, "").toLowerCase();
      });
    }),
    document.querySelectorAll(".ps-alphanumericspace").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9 ]/gi, "");
      });
    }),
    document.querySelectorAll(".ps-alphanumericspace-u").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9 ]/gi, "").toUpperCase();
      });
    }),
    document.querySelectorAll(".ps-alphanumericspace-l").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9 ]/gi, "").toLowerCase();
      });
    }),
    document.querySelectorAll(".ps-email").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9._@]/gi, "");
      });
    }),
    document.querySelectorAll(".ps-email-u").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9._@]/gi, "").toUpperCase();
      });
    }),
    document.querySelectorAll(".ps-email-l").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9._@]/gi, "").toLowerCase();
      });
    }),
    document.querySelectorAll(".ps-address").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9&. ,()-/]/gi, "");
      });
    }),
    document.querySelectorAll(".ps-address-u").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9&. ,()-/]/gi, "").toUpperCase();
      });
    }),
    document.querySelectorAll(".ps-address-l").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^a-z0-9&. ,()-/]/gi, "").toLowerCase();
      });
    }),
    document.querySelectorAll(".ps-integer").forEach((a) => {
      a.addEventListener("input", () => {
        a.value = a.value.replace(/[^0-9]/gi, "");
      });
    });
});
